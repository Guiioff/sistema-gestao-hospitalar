spring.application.name=autenticacao
server.port=${server_port}
spring.datasource.url=${datasource_url}
spring.datasource.username=${datasource_username}
spring.datasource.password=${datasource_password}
spring.jpa.hibernate.ddl-auto=update
security.caminho-chave-privada=${caminho_chave_privada}
security.caminho-chave-publica=${caminho_chave_publica}
spring.rabbitmq.host=${rabbitmq_host}
spring.rabbitmq.port=${rabbitmq_port}
spring.rabbitmq.username=${rabbitmq_username}
spring.rabbitmq.password=${rabbitmq_password}
spring.rabbitmq.virtual-host=${rabbitmq_vhost}
spring.rabbitmq.ssl.enabled=${rabbitmq_ssl-enabled}
rabbitmq.queue.novo-usuario=${rabbitmq_novo_usuario_queue}
security.jwt.token-issuer=${jwt.token-issuer}
security.jwt.duracao-token-segundos=${jwt.duracao-token-segundos}
spring.rabbitmq.listener.simple.acknowledge-mode=${rabbitmq_acknowledge_mode}
security.refresh-token-duracao-segundos=${refresh_token_duracao_segundos}
rabbitmq.queue.novo-usuario-dlq=${rabbitmq_novo_usuario_dlq}
rabbitmq.exchange.novo-usuario=${rabbitmq_exchange_novo_usuario}